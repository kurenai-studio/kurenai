export namespace preferences {
    namespace scene_cache {
        let use: string;
        let interval: string;
    }
    namespace scene {
        let debug_native: string;
        let native: string;
        let on_native_change: string;
        let tick: string;
        let multi: string;
    }
}
